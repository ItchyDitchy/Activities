#include <bits/stdc++.h>

std::string parkingSpots[26][4][17];
int status = 0;

void parkCar();

int main() {
    while (status != 0) {

    }
    system("cls");
    std::cout << "Thank you for using this parking program.";
    return 0;
}

void parkCar() {
    std::string name = "";
    do {
        system("cls");

    } while (name == "" && 3 >= name.length() && name.length() >= 16);
}